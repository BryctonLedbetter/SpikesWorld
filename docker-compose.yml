version: '3.8'

services:
  web:
    build: .
    container_name: new-website
    ports:
      - "8000:8000"
    volumes:
      - .:/app
    environment:
      - FLASK_ENV=development
    restart: always
